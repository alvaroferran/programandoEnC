#include "español.h"

entero principal(vacío){

    entero número;

    por (número=0; número<10; número++){
        si(número==7) {
            continúa;
        }
        imprime("Iteración %d\n", número);
    }

    devuelve 0;
}
